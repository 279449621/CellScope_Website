version: 2

build:
  os: ubuntu-22.04  # 推荐使用更新的 OS
  tools:
    python: "3.10"   # 使用较新的 Python 版本

python:
  install:
    - requirements: requirements.txt  # 确保路径正确

sphinx:
  configuration: source/conf.py  # 确认路径存在